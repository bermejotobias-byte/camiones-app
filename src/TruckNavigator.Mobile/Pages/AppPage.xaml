<?xml version="1.0" encoding="utf-8" ?>
<!--
    La aplicacion entera es la web que sirve la API. Esta pagina es la cascara:
    hospeda el WebView a pantalla completa y resuelve lo que el navegador de
    adentro no puede hacer solo — la URL del backend, el GPS y el discador.

    El panel de configuracion aparece unicamente cuando el backend no responde.
    No hay una pantalla nativa permanente: si la web anda, la cascara no se ve.
-->
<ContentPage
    x:Class="TruckNavigator.Mobile.Pages.AppPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.NavBarIsVisible="False"
    BackgroundColor="#0d1418">

    <Grid>

        <HybridWebView
            x:Name="WebView"
            HybridRoot="wwwroot"
            DefaultFile="index.html"
            RawMessageReceived="OnMessageReceived" />

        <!-- Se muestra mientras se verifica que el backend conteste. -->
        <VerticalStackLayout
            x:Name="StartupPanel"
            BackgroundColor="#0d1418"
            VerticalOptions="Fill"
            HorizontalOptions="Fill"
            Padding="28"
            Spacing="16">

            <VerticalStackLayout VerticalOptions="Center" Spacing="16">

                <ActivityIndicator
                    x:Name="StartupSpinner"
                    IsRunning="True"
                    Color="#35b8e8"
                    HeightRequest="36" />

                <Label
                    x:Name="StartupLabel"
                    Text="Conectando…"
                    TextColor="#a8bcc7"
                    FontSize="15"
                    HorizontalTextAlignment="Center" />

                <!-- Escape hatch: solo visible si no se pudo conectar. -->
                <VerticalStackLayout x:Name="ServerSetup" IsVisible="False" Spacing="12">

                    <Label
                        Text="Dirección del servidor"
                        TextColor="#6f8794"
                        FontSize="12"
                        FontAttributes="Bold" />

                    <Entry
                        x:Name="ServerEntry"
                        Placeholder="http://192.168.1.60:5080"
                        Keyboard="Url"
                        TextColor="#eaf1f5"
                        PlaceholderColor="#6f8794"
                        BackgroundColor="#1e2b33" />

                    <Button
                        Text="Reintentar"
                        Clicked="OnRetry"
                        BackgroundColor="#35b8e8"
                        TextColor="#04222f"
                        FontAttributes="Bold"
                        HeightRequest="50" />

                    <!--
                        Salida de emergencia. Sin esto, una direccion guardada que
                        dejo de servir no se puede descartar sin adivinar cual era
                        la original, y la app queda inutilizable.
                    -->
                    <Button
                        Text="Usar la dirección de fábrica"
                        Clicked="OnUseFactoryUrl"
                        BackgroundColor="Transparent"
                        BorderColor="#24333c"
                        BorderWidth="1"
                        TextColor="#a8bcc7"
                        HeightRequest="46" />

                    <Label
                        TextColor="#6f8794"
                        FontSize="12.5"
                        LineHeight="1.35"
                        Text="Podés escribir sólo la IP y el puerto, por ejemplo 192.168.1.5:5080. Revisá, en este orden: que el backend esté corriendo; que el teléfono esté en la misma red Wi-Fi que la computadora; y que la IP sea la de ahora — cambia cada vez que la computadora se reconecta." />

                </VerticalStackLayout>

            </VerticalStackLayout>

        </VerticalStackLayout>

    </Grid>

</ContentPage>
