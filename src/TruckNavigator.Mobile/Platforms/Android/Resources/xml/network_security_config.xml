<?xml version="1.0" encoding="utf-8"?>
<!--
  Android bloquea HTTP sin TLS por defecto.

  Esta excepcion existe SOLO para desarrollo: permite que la app llegue al
  backend que corre en la maquina de desarrollo por la red local. Antes de
  distribuir la app hay que servir la API por HTTPS y eliminar este archivo
  junto con el atributo android:networkSecurityConfig del manifest.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.60</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
